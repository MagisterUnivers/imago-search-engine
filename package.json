{
  "name": "imago-finder",
  "version": "1.0.0",
  "author": "MagisterUnivers",
  "license": "MIT",
  "scripts": {
    "docker:run": "docker-compose down -v && docker-compose build --no-cache && docker-compose up -d"
  }
}
