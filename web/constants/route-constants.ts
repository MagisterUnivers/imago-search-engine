export const ISO_DATE_RE = /^\d{4}-\d{2}-\d{2}$/;
export const SORT_VALUES = new Set(["asc", "desc", "none"]);
export const MAX_QUERY_LENGTH = 200;
export const MAX_PAGE_SIZE = 100;
export const DEFAULT_PAGE_SIZE = 20;
